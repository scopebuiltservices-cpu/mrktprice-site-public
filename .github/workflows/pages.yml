name: Build + publish MrktPrice

# SINGLE self-publishing pipeline for mrktprice.com.
#
# Why this exists: the site used to be published from a SEPARATE private repo via a
# Personal Access Token (SITE_DEPLOY_TOKEN). That PAT was the fragile link - it expires,
# can be deleted, depends on an exact secret name, and when missing the build went green
# but silently stopped publishing. This workflow removes that entire failure class: it
# builds the data AND commits it to THIS repo using the built-in GITHUB_TOKEN (which
# GitHub injects on every run, auto-rotates, and cannot be deleted or misnamed), then
# deploys to Pages. No cross-repo PAT anywhere.
#
# Triggers:
#   push              -> redeploy the committed site (content edits like terminal.html)
#   schedule          -> nightly: rebuild live Market Map + xsection + history, commit, deploy
#   workflow_dispatch -> same as nightly, on demand
#
# Integrity philosophy: CORE-data failures (SAMPLE fallback, thin universe, stale/truncated
# JSON) BLOCK the publish; ENRICHMENT degradation (FMP/EODHD down) is surfaced loudly but
# still publishes prices/charts/macro. See tools/market_map/publish_guards.py.

on:
  push:
    branches: [main]
  schedule:
    - cron: "20 6 * * 1-6"     # nightly EOD rebuild (~01:20-02:20 America/New_York)
    - cron: "0 19 * * 1-5"     # 3pm ET intraday refresh during EDT (Mar-Nov)  -> 19:00 UTC
    - cron: "0 20 * * 1-5"     # 3pm ET intraday refresh during EST (Nov-Mar)  -> 20:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: site
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }   # full history so the change-detector can diff the whole push range
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }

      # ROOT-CAUSE FIX: a push that changes the build engine but NOT .rebuild (and has no
      # [rebuild] tag) used to deploy stale data — the new code sat committed but never executed.
      # This step rebuilds whenever ANY file under tools/market_map/ (engine + connectors) changes,
      # so "push new engine code" always regenerates the published data. Manual triggers kept as backup.
      - name: Detect whether a data rebuild is needed
        id: detect
        run: |
          need=false
          ev="${{ github.event_name }}"
          if [ "$ev" = "workflow_dispatch" ]; then
            need=true
          elif [ "$ev" = "schedule" ]; then
            need=true
            # Two intraday crons fire at 19:00 and 20:00 UTC; keep ONLY the one that lands on 15:00
            # America/New_York so we get exactly ONE 3pm-ET refresh year-round (DST-correct).
            uth=$(date -u +%H)
            if [ "$uth" = "19" ] || [ "$uth" = "20" ]; then
              eth=$(TZ='America/New_York' date +%H)
              if [ "$eth" != "15" ]; then need=false; echo "::notice title=Intraday skip::cron at ${uth}:00 UTC = ${eth}:00 ET (not 3pm ET) — skipping the duplicate DST slot"; fi
            fi
          else
            before="${{ github.event.before }}"
            if [ -z "$before" ] || [ "$before" = "0000000000000000000000000000000000000000" ]; then
              changed=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
            else
              changed=$(git diff --name-only "$before" "${{ github.sha }}" 2>/dev/null || git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
            fi
            echo "changed files in push:"; echo "$changed"
            if echo "$changed" | grep -qE '^tools/market_map/|^\.rebuild$'; then need=true; fi
            if printf '%s' "${{ github.event.head_commit.message }}" | grep -q '\[rebuild\]'; then need=true; fi
          fi
          echo "need=$need" >> "$GITHUB_OUTPUT"
          echo "::notice title=Rebuild decision::data rebuild needed = $need"

      - name: Refresh Market Map (live) + xsection + static history
        if: steps.detect.outputs.need == 'true'
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
          TWELVEDATA_API_KEY: ${{ secrets.TWELVEDATA_API_KEY }}
          FMP_API_KEY: ${{ secrets.FMP_API_KEY || secrets.FMP_ULTIMATE_API_KEY || secrets.FMP_UTIMATE_API_KEY }}
          EODHD_API_KEY: ${{ secrets.EODHD_API_KEY }}
          ALPACA_API_KEY_ID: ${{ secrets.ALPACA_API_KEY_ID }}
          ALPACA_API_SECRET_KEY: ${{ secrets.ALPACA_API_SECRET_KEY }}
          TIINGO_API_KEY: ${{ secrets.TIINGO_API_KEY }}
          ALPHAVANTAGE_API_KEY: ${{ secrets.ALPHAVANTAGE_API_KEY }}
          MARKETAUX_API_KEY: ${{ secrets.MARKETAUX_API_KEY }}
          MRKT_FETCH_CURVE: "1"
          RUSSELL_LIMIT: "120"
        run: |
          python -m pip install --quiet --upgrade pip
          pip install --quiet -r tools/market_map/requirements.txt
          mkdir -p /tmp/mmbuild
          cd tools/market_map
          if python build_market_map.py --real --out /tmp/mmbuild/marketmap.json; then
            python publish_guards.py /tmp/mmbuild/marketmap.json
            # CONTRACT GATE: validate the payload against marketmap.schema.json + invariants (version,
            # coverage, quantile non-crossing, finite). A violation BLOCKS publish - never ship a payload
            # the consumer would have to render as no-call.
            python validate_payload.py /tmp/mmbuild/marketmap.json --min-names 30 || { echo "::error::contract violated - refusing to publish"; exit 1; }
            # QA SIGN-OFF: final release gate — ties the freshly-built artifacts to a sha256 fingerprint of
            # ALL engine source (provenance), and hard-gates universe size, freshness, non-sample source,
            # quantile non-crossing, finite scores, and core coverage. A FAIL refuses the publish. The
            # signed manifest is retained in the run log + shipped to _site/.build for audit.
            python qa_signoff.py /tmp/mmbuild/marketmap.json --also /tmp/mmbuild/xsection.json \
              --src-root . --min-names 30 --max-age-days 2 --min-core-cov 0.70 --schema-version 1.0 \
              --out /tmp/mmbuild/qa-signoff.json || { echo "::error::QA sign-off FAILED - refusing to publish"; exit 1; }
            cp /tmp/mmbuild/marketmap.json "$GITHUB_WORKSPACE/marketmap.json"
            mkdir -p "$GITHUB_WORKSPACE/.build" && cp /tmp/mmbuild/qa-signoff.json "$GITHUB_WORKSPACE/.build/qa-signoff.json" 2>/dev/null || true
            cp /tmp/mmbuild/xsection.json  "$GITHUB_WORKSPACE/xsection.json" 2>/dev/null || echo "::warning::xsection.json not produced this run"
            cd "$GITHUB_WORKSPACE"
            python tools/market_map/emit_static.py --universe marketmap.json --out . --hist --cap 250 || echo "::warning::history emit partial"
            # KEYLESS: refresh cik.json (ticker->CIK) from SEC's free company_tickers.json so the
            # terminal can pull CONFIRMED quarterly report dates client-side for the whole universe.
            python tools/market_map/emit_cik.py --out cik.json --marketmap marketmap.json --cards cards || echo "::warning::cik.json refresh skipped"
            # DATA-DRIVEN UNIVERSE: emit universe.json (membership + sector census + dq/drift rollup) from the map
            python tools/market_map/emit_universe.py --map marketmap.json --out universe.json || echo "::warning::universe.json emit skipped"
            # EVENT-AWARE: high-impact US macro calendar (FMP) -> events.json (served + committed)
            FMP_API_KEY="$FMP_API_KEY" python tools/market_map/events_calendar.py events.json || echo "::warning::events.json emit skipped"
            # KEYLESS feedback loop: log today's Bull/Bear α + attach matured realized returns + refit
            # so the board reads α as an empirically-calibrated expected return % (accumulates nightly).
            python tools/market_map/alpha_calib.py --mm marketmap.json --hist hist --log data/alpha_log.jsonl --out alpha_calib.json --horizon 21 || echo "::warning::alpha calibration skipped"
            # KEYLESS continuous monitoring: read our OWN logs (alpha_log/health_log/factor_ic) ->
            # rank-IC, decile calibration, alpha-PSI drift, spread-DSR, data-health trend + alert flags
            # -> monitoring/latest.json (shipped for the audit page; never fails the build).
            python tools/market_map/monitoring.py --root . --out monitoring/latest.json || echo "::warning::monitoring skipped"
            # GUARD: loud signal if the engine ran but the FMP history layer produced nothing
            # (catches "new code committed but didn't execute / connect").
            FMP_API_KEY="$FMP_API_KEY" python tools/market_map/verify_history.py marketmap.json || true
            # HARDENED ARTIFACT GUARD — accept a promoted artifact only after re-reading the final
            # bytes from the authoritative path. Catches a truncated/empty/half-written promote that
            # JSON-validity alone can miss. Runs in the CI runtime (no mount illusion). Hard-gate the
            # primary payload; warn on the secondary ones so a thin day still publishes the map.
            python tools/verify_artifact.py guard marketmap.json --min-bytes 5000 --ends-with '}' || { echo "::error::marketmap.json failed the hardened artifact guard"; exit 1; }
            [ -f cik.json ]       && { python tools/verify_artifact.py guard cik.json       --min-bytes 100 --ends-with '}' || echo "::warning::cik.json guard"; }
            [ -f xsection.json ]  && { python tools/verify_artifact.py guard xsection.json  --min-bytes 100 --ends-with '}' || echo "::warning::xsection.json guard"; }
            [ -f alpha_calib.json ] && { python tools/verify_artifact.py guard alpha_calib.json --min-bytes 20 --ends-with '}' || echo "::warning::alpha_calib.json guard"; }
            # Forensics layer: a sha256/byte/tail manifest of the promoted set (retained in the run log).
            mkdir -p .build && python tools/verify_artifact.py manifest marketmap.json cik.json xsection.json alpha_calib.json --out .build/artifact-manifest.json 2>/dev/null || true
          else
            echo "::error::Real Market Map build FAILED - refusing to publish synthetic data"; exit 1
          fi

      - name: Commit refreshed data (self-publish, no PAT)
        if: steps.detect.outputs.need == 'true'
        run: |
          git config user.name  "mrktprice-bot"
          git config user.email "bot@mrktprice.com"
          git add -A marketmap.json xsection.json universe.json events.json hist/ monitoring/latest.json data/alpha_log.jsonl tools/market_map/est_history.jsonl tools/market_map/factor_snapshots.jsonl tools/market_map/factor_ic.jsonl tools/market_map/trig_snap.jsonl tools/market_map/trig_out.jsonl tools/market_map/drift_ref.json tools/market_map/drift_store.jsonl tools/market_map/trial_ledger.jsonl tools/market_map/health_log.jsonl 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "::notice::no data changes to commit"
          else
            git commit -m "Market Map nightly: refresh marketmap.json + xsection.json + hist/ ($(date -u +%FT%TZ))"
            git push
            echo "published $(git rev-parse --short HEAD)"
          fi

      - uses: actions/configure-pages@v5

      - name: Rebuild reports index + stage site
        run: |
          python build_reports.py || echo "::warning::reports index rebuild skipped"
          mkdir -p _site/reports
          for f in *.html *.js robots.txt sitemap.xml marketmap.json xsection.json universe.json events.json marketmap.schema.json xsection.schema.json; do [ -f "$f" ] && cp "$f" "_site/$f" || true; done
          [ -f monitoring/latest.json ] && { mkdir -p _site/monitoring && cp monitoring/latest.json _site/monitoring/latest.json; } || true
          mkdir -p _site/.build && cp .build/qa-signoff.json _site/.build/ 2>/dev/null || true   # ship the signed QA manifest for audit
          cp -r reports/*.html _site/reports/ 2>/dev/null || true
          rm -f _site/reports/report-template.html
          cp -r hist _site/hist 2>/dev/null || true
          cp -r splash _site/splash 2>/dev/null || true
          [ -f CNAME ] && cp CNAME _site/CNAME || echo "mrktprice.com" > _site/CNAME
          touch _site/.nojekyll

      - name: Emit static per-ticker cards (no network) + augment from committed hist
        run: python tools/market_map/emit_static.py --universe _site/marketmap.json --out _site --hist-dir _site/hist || echo "::warning::card emit skipped"

      - name: Integrity gate - inline scripts + JSON not truncated
        run: |
          node tools/check-scripts.mjs
          node tools/check-external-js.mjs              # syntax-gate every external <script src> module (engine/panels)
          node tools/check-file-budget.mjs              # fail if any source file approaches the mount/read boundary (keeps modules small + verifiable)
          node tools/check-duplication.mjs              # fail if a canonical risk/return estimator is redefined outside metrics.py (no divergent duplicates)
          python tools/market_map/integrity_manifest.py --check   # content-integrity tripwire: NUL/truncation/vanished-definition on the critical surface
          python -m py_compile $(find tools -name '*.py')   # fast syntax gate for ALL python (catches the monolith even on content-only pushes)
          python tools/market_map/check_json.py _site/marketmap.json _site/xsection.json
          [ -f _site/xsection.json ] && python tools/market_map/validate_xsection.py _site/xsection.json || echo "::notice::xsection.json absent this run"
          # secondary artifact contracts (cik/alpha_calib/events/universe) — absent files are skipped
          ARTS=""; for a in cik.json alpha_calib.json events.json universe.json; do [ -f "_site/$a" ] && ARTS="$ARTS _site/$a"; done
          if [ -n "$ARTS" ]; then
            python -m pip install --quiet jsonschema || true     # enables the declarative JSON Schema gate
            python tools/market_map/schema_validate.py $ARTS      # Draft 2020-12 shape/type/enum contract
            python tools/market_map/validate_artifacts.py $ARTS   # cross-field invariants (count==len, dup tickers)
          else echo "::notice::no secondary artifacts this run"; fi
          # coverage-regression alarm: fail if a data domain dropped to 0% vs the prior run (silent broken feed)
          HL="$(find _site . -name health_log.jsonl 2>/dev/null | head -1)"
          [ -n "$HL" ] && python tools/market_map/coverage_regression.py "$HL" || echo "::notice::health_log.jsonl absent this run"

      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with: { path: '_site' }

      - id: deployment
        uses: actions/deploy-pages@v4
